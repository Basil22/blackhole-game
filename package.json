{
  "name": "black-hole",
  "version": "1.0.0",
  "description": "Throw objects into a black hole and watch real spaghettification",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "python3 serve.py 8000",
    "test": "node tests/run_all.js",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android",
    "build:android": "npx cap sync && cd android && ./gradlew assembleDebug",
    "build:android:release": "npx cap sync && cd android && ./gradlew assembleRelease",
    "build:aab": "npx cap sync && cd android && ./gradlew bundleRelease"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/share": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/app": "^8.1.1"
  }
}
